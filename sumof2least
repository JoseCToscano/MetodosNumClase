"""
Create a function that takes a list of numbers and returns the sum of the two lowest positive numbers.
"""

def sum_two_smallest_nums(lst):
	lst = [num for num in lst if num > 0]
	least= min(lst)	
	lst.remove(least)
	return	least + min(lst)
	
