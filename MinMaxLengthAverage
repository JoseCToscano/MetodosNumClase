"""
Create a function that takes a list of numbers and returns the following statistics:
"""

def minMaxLengthAverage(lst):
	answer=[min(lst), max(lst), len(lst), (sum(lst)/len(lst))]
	return answer
	
