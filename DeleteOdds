"""
Create a function that takes a list of numbers and returns only the even values
"""

def noOdds(lst):
	newlst = []
	i = 0
	while i < len(lst):
			if lst[i] % 2 == 0:		
				newlst.append(lst[i])
			i = i + 1
	return newlst
	
